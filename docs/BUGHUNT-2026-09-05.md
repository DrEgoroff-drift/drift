# Bug hunt 2026-09-05 (wf_c25e8341: 115 Haiku scans, 167 raw findings)

Sonnet verification survived for 3 of 11 batches; the remaining 116 findings were reviewed by hand in the follow-up session.
Verdict: 22 real, the rest false (function hoisting in the concatenated build, valid `x?.5:1` syntax, fillRect with negative height is legal, G.* fields initialised in 08-state).

## Real - game code
| # | Where | What |
|---|---|---|
| 1 | src/05b-instr-kit.js:96 | `instrKnock` is declared inside `instrWearRu`; the global 13-pirates.js:161 guards with typeof and never fires. M127 hit-knockout mechanic is dead. |
| 2 | src/14-save.js snapshot | `G.soldTotal` is never written or restored. Coop exam turnover resets on every reload. |
| 3 | src/21c-built.js:22 | Looks up `G.bases[builtKey(sx,sy)]` ("sx,sy") but bases are stored under `baseKey` ("sx,sy:idx"). "ВАША БАЗА" mark never appears. `B.pIdx` is never set anywhere. |
| 4 | src/29g-winter-draw.js:609 vs :776 | Fault lamps drawn at `+H*.026+i*H*.038, y=.14h`, hit-tested at `+H*.018+i*H*.030, y=.10h`. Tap misses the lamp. |
| 5 | src/24c-mode-wanderer-draw.js:178 | `A.y-D.y` is negative (near ceiling edge is higher on screen), so all 14 snow dots sit on one line at `D.y`. |
| 6 | src/11z-misclosure.js:77 | Strips restored raw (14-save.js:677); strips saved before `mis` existed give NaN amplitude, figure segment silently skipped. |
| 7 | src/12y-parrot-face.js:723 (+site/parrot.js:732, treplo.html:1495) | Pointer handler ignores the `PAR.hang*150` translate used at draw; pokes miss while the bird hangs. |
| 8 | src/17c-system-draw.js:345 | `((i*3)%3)` is always 0; the width variation is dead. |
| 9 | src/12pa-beacon.js:173 | `G.mode==="road"` never happens (road is a body class, not a mode). Dead clause, harmless. |
| 10 | src/28-loop.js:349 | `prof()` lacks wanderer/winter/spa handlers (dev tool). |

## Real - site / bird / scripts
| # | Where | What |
|---|---|---|
| 11 | site/api.php:845 | `foreach ... if` without braces: `$row['mv']` and `$outL[] = $row` run once after the loop. Inbox delivers only the last card; several cards in `in` lose all but one. |
| 12 | site/parrot.js:1030, site/treplo.html:1794 | Standalone cage handler scales with `H/272`, drawing uses `H/304`. |
| 13 | site/treplo3d.html:2717 | `renderSize` recreates HDR/bloom targets on every resize without deleting old ones (no `deleteFramebuffer` anywhere). |
| 14 | deploy.ps1:27, dev.ps1:24, dev.ps1:105 | Child build.ps1/bird.ps1 exit code not checked; a failed build still scp's the stale drift.html. |
| 15 | docs/lookrun.ps1:31 | Stop-Probe kills chrome.exe only; the msedge fallback picked at line 19 is never stopped. |
| 16 | docs/mkview.ps1:415 | Second `scene==="hold"` branch unreachable (first at :57). |
| 17 | docs/stand.ps1:55 | Unescaped `.` in legacy-upload regex. |

## Real - tests that cannot fail
| Where | Pattern |
|---|---|
| tests/91c-mgr.js:84, 91g-relic.js:73, 91zzzt-chess.js:37, 91zzzz-fuzz.js:179 | `... \|\| true` |
| tests/91zzzt-chess.js:43, 91zzd-grown.js:29 | value compared with itself |
| tests/91k-home.js:146 | `cond ? true : true` |
| tests/91m-nodes.js:170 | comment says "last three", code takes `slice(0,3)` |
| tests/91z-missile.js:59 | `mslFire()===false \|\| cargo===1` should be `&&` |
| tests/91zp-stories.js:76,78; 91zzzw-relay.js:157 | `ok(true)` after a draw call |
| tests/91zzzzzu-monotone.js:100 | comment says 65% floor, asserts 60% |

## False (categories, with counts)
- "called without typeof guard" / "existence guard" on top-level functions: 18 — build.ps1 concatenates every src file into one scope; declarations hoist.
- `x?.5:1` "optional chaining": 5 — spec forbids `?.` before a digit; parses as ternary.
- fillRect with negative height: 5 — canvas normalises negative rects.
- G.owned / G.uniqueShips / G.scrip / G.species / G.seen "may be undefined": 9 — initialised in 08-state.js and re-guaranteed in applySave.
- Dead `*0` multipliers, BOM/Join-Path style, 40px touch targets, plural forms ("спичек: N", "открылись главы"), ellipse arc direction, water at max `tr.h` (y grows downward, so max is deepest): all judged intentional or cosmetic.
